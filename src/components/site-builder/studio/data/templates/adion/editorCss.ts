export const adionEditorCss = `
  [data-template-id="adion-framion-inspired"],
  [data-template-id="adion"] {
    font-family:
      Inter,
      ui-sans-serif,
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      sans-serif;
  }

  [data-template-id="adion-framion-inspired"] img,
  [data-template-id="adion"] img {
    max-width: 100%;
  }
`;

export default adionEditorCss;